const config = {
  DB: {
    nestjs_beginner: {
      MONGO_URI: 'mongodb://localhost:27017/nestjs_beginner',
    },
  },
  SERVER: {
    PORT: 8080,
    ENV: 'development',
  },
};

export default config;
